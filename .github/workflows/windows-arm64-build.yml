name: Windows ARM64 Build (Fast, No Minification)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20.18.1

      - name: Install dependencies
        run: npm ci

      - name: Build React
        run: npm run buildreact

      - name: Compile source
        run: npm run compile

      - name: Run gulp for Windows ARM64
        run: npm run gulp vscode-win32-arm64

      - name: Zip .build/ output
        run: |
          powershell -Command "Compress-Archive -Path .build\* -DestinationPath void-win32-arm64.zip"

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: void-win32-arm64
          path: |
            .build/
            void-win32-arm64.zip
